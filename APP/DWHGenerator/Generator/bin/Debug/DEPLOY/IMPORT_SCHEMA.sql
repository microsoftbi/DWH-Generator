--TRUNCATE TABLE META.DBO.ATTRIBUTE

INSERT INTO META.DBO.ATTRIBUTE
(
    TABLE_CATALOG,
    TABLE_NAME,
    COLUMN_NAME,
    DATA_TYPE,
    CHARACTER_MAXIMUM_LENGTH,
    NUMERIC_PRECISION,
    NUMERIC_SCALE
)
SELECT
    'MDS',
    TABLE_NAME,
    COLUMN_NAME,
    DATA_TYPE,
    CHARACTER_MAXIMUM_LENGTH,
    NUMERIC_PRECISION,
    NUMERIC_SCALE
FROM IMP.INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_NAME IN ('DEALER_TYPE')
AND COLUMN_NAME NOT IN ('FULLY_QUALIFIED_FILE_NAME','FILE_TRANSFER_DTS','LOAD_DTS','REC_SRC')
